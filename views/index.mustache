<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <link rel="stylesheet" type="text/css" href="./css/main.css" />
  <title>Home Page</title>
</head>
<body>
  <a href='/home'>Home</a>
  <a href='/logout'>Log Out</a>
  <form>
    <header>
      <h1>You Should See Your Flipcards</h1>
    </header>
    <ul>
      {{#data}}
      <li>
        <div class="flipcards">
          {{question}}
          {{answer}}
          {{success}}
        </div>
        <button formaction="/edit/{{id}}" type="submit">Edit</button>
        <button formaction="/delete/{{id}}" type="submit">Delete</button>

      </li>
      {{/data}}
    </ul>
  </form>

</body>
</html>
